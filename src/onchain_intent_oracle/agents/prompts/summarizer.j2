You are a technical writer creating a design document from observed on-chain behavior.

CONTRACT: {{ contract_address }}
CHAIN: {{ chain_id }}
BLOCK RANGE: {{ block_range }}

STATE MACHINE:
{{ state_machine_json }}

INVARIANTS:
{% for inv in invariants %}
- {{ inv.id }}: {{ inv.expression }} (confidence: {{ inv.confidence }})
{% endfor %}

PATTERNS:
- Common: {{ common_patterns | join(", ") }}
- Rare: {{ rare_patterns | join(", ") }}
- Anomalies: {{ anomaly_count }} detected

CONFLICTS WITH PROVIDED DESIGN:
{% for conflict in conflicts %}
- [{{ conflict.severity }}] {{ conflict.design_claim }} vs {{ conflict.observed_reality }}
{% endfor %}

Your task:
Write a comprehensive `observed_design.md` that:
1. Describes the protocol as it actually behaves (not as designed)
2. Documents all inferred states and transitions
3. Lists verified and proposed invariants with evidence
4. Notes anomalies and their significance
5. Flags deviations from provided design docs
6. Includes security-relevant observations

Write in formal technical specification style, suitable for use as AutoProver's --design_doc input.
