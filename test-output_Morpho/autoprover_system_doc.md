# Contract 0xBBBBBbbBBb9cC5e90e3b3Af64bdAF62C37EEFFCb

This document describes **observed on-chain behavior** of `0xBBBBBbbBBb9cC5e90e3b3Af64bdAF62C37EEFFCb` (chain ID 1) over blocks 18883124-18883954 (12 transaction(s) analyzed), generated by OnChainIntentOracle.

**This is a cross-check against reality, not a replacement for a hand-written design doc.** Everything below reflects what actually happened on-chain in the analyzed window, with evidence (transaction hashes, confidence levels, sample sizes) behind every claim -- not what the contract is intended or documented to do. If this diverges from an existing design doc or NatSpec comments, treat the divergence as a signal to investigate which one is stale: documentation can go out of date the moment code changes without an update; on-chain behavior cannot.

## Contract 0xBBBBBbbBBb9cC5e90e3b3Af64bdAF62C37EEFFCb

### Entry points observed on-chain

`enableIrm()`, `enableLltv()`, `setOwner()`

_Only methods with at least one observed call in the analyzed range are listed. The contract's full interface may include additional functions never called here._

### State observed on-chain

_No storage-level state changes were observed with evidence (a fetched trace or state diff) in this range -- this does not mean the contract is stateless, only that no evidenced state transition was captured here._

### Interactions

12 call(s) observed, all directly against this contract's own address.

### Requirements (observed, not proven)

Each item below is a statistical observation over the analyzed sample, not a formal guarantee -- confidence and sample size are given so you can judge how much weight to put on each one. None of these should be treated as already verified; they're candidates for the Certora Prover to actually check.

- **msg.value == 0 for all observed txs** -- high confidence (0.95) from on-chain observation over the analyzed block range. Contract does not accept ETH transfers
- **msg.sender == 0x937ce2d6c488b361825d2db5e8a70e26d48afed5 for enableLltv()** (observed in 9/9 calls) -- high confidence (1.00) from on-chain observation over the analyzed block range. enableLltv() called exclusively by 0x937ce2d6c488b361825d2db5e8a70e26d48afed5
- **enableLltv() never reverts** -- medium confidence (0.90) from on-chain observation over the analyzed block range. Observed 9 calls (via tx receipts), all succeeded
- **all txs from 0x937ce2d6c488b361825d2db5e8a70e26d48afed5** -- medium confidence (0.80) from on-chain observation over the analyzed block range. Single caller observed - possible test or controlled environment

_3 observed function(s) had no corresponding claim in any design doc provided to this analysis run (see `conflict_report.md` for details, if one was generated)._
